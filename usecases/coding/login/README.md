# Login Page (静态示例)

一个纯前端的登录页示例（HTML/CSS/JS），用于 OpenClaw usecase：coding。

## 运行

直接用浏览器打开：

- `usecases/coding/login/index.html`

或用任意静态服务器：

```bash
python3 -m http.server 8000
# 然后访问 http://localhost:8000/usecases/coding/login/
```

## 说明

- 这是演示页面：不包含真实后端认证。
- 包含简单的表单校验与“显示/隐藏密码”。
